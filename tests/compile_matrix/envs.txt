# tests/compile_matrix/envs.txt
# Обязательные env ядра (отсутствие -> fail).
#
# L3 собирает только «ядро без внешних компонентов»: TestCore32 (extends esp32)
# и TestCore8266 (extends esp8266). Этого достаточно для AC-19/AC-22 и проверки
# совместимости ядра, и это существенно быстрее полной матрицы.
TestCore32
TestCore8266

# В будущем может понадобиться расширить матрицу на полные env
# (сейчас исключены намеренно, чтобы не собирать полные прошивки):
#   esp32
#   esp8266
#   esp32cam
#   esp32-c3-devkitm-1
# Для возврата — раскомментировать строки выше (env должны быть определены в
# targets/*.ini) либо перенести их в опциональную секцию.
